import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-year-in-review',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
